状态：草稿

申请人：
活动日期：
活动时间：
校区：
教学楼：
教室：

---

<!-- ↓↓↓ 填写说明：提交前请把下面全部内容删除 ↓↓↓ -->

## 填写说明

1. **文档标题就是活动名称**（如「新生见面会」「思维训练营第 3 期」）。
   不要用「无标题文档」，也不要叫「指导文档」「审批日志」这类系统文档名。
2. 填完把第一行改成 `状态：待提交`，agent 才会处理。
3. 字段名不要改，冒号后面都要有内容（教学楼、教室除外）。
4. 活动日期写成 `2026-09-16`；活动时间写成 `16:10-18:00`。
5. 校区填 `鼓楼` / `浦口` / `仙林` / `苏州`。
6. 教学楼、教室不填就随机分配。
7. 活动开始时间必须距今 **≥ 48 小时**。

> agent 处理后会把第一行改成 `已提交（待审核通过）` 或
> `已退回（修改后请把状态改为待提交）`，这是正常的。
